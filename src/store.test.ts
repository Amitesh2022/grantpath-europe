import{setActivePinia,createPinia}from'pinia';import{beforeEach,describe,expect,it}from'vitest';import{useGrantStore}from'./store';describe('grant portfolio',()=>{beforeEach(()=>setActivePinia(createPinia()));it('shortlists a funding call',()=>{const s=useGrantStore();s.toggleSaved(1);expect(s.saved).toContain(1)});it('creates a bid workspace',()=>{const s=useGrantStore();s.createBid('Shared data space','Digital Europe',5);expect(s.cohorts[0].seats).toBe(5)})})
